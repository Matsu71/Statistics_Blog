# scripts ディレクトリ

将来的に Codex や GitHub Actions から呼び出す自動化スクリプトの置き場です。

想定用途:

- 未作成の用語を選んで `src/content/terms/` に Markdown を追加する
- 用語一覧の下書きデータを整形する
- 公開前チェックを自動化する

この初期段階では、まだ実行スクリプトは置かず、拡張ポイントだけ確保しています。
