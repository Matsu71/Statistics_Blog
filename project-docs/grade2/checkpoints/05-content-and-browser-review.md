# 2級の追加点検：標本サイズの条件・数字の可読性・リンク検証

確認日2026-09-11。実行34587735750の取得した成果物を確認。基礎40講座の898項目と、学習機能のChromium/WebKit470項目は成功。2級の全体検証は同一文書のフラグメント移動で`page.goto`がnullを返すことをテストが扱っておらず停止した。HTTP応答とDOM上のリンク先を別々に検査するよう修正し、検査対象は削減しない。

## 教材本文の追加確認

全G01〜G40の説明、問題、解答、式と使用条件を読み直した。G26の一側・分散既知の標本サイズでは、不等式の右辺を二乗する前の正値条件を補足した。通常の目標検出力がαより大きい場合の式と、目標がα以下の場合の最小正整数を分けて示す。n=24/25の80%境界と、目標がα以下の場合の2件を別実装で追加検算し、合計145件の数値照合がローカルで成功した。公開候補としての全件再検証は別に実施する。

## 表示の確認

取得済み生成HTMLをオフラインで表示し、G03ローレンツ曲線、G33回帰図、G39実データ集計表を確認。G39の小数末尾が狭い表で折り返されていたため、2級ページだけのCSSで数値セルを改行させず、表内スクロールで読むよう修正する。横長の表にはキーボードでのスクロール入口を付ける。基礎の原稿は変更しない。

テストには数字の途中改行の検出を加え、G25・G26・G35・G38・G40のスクリーンショットも保存する。スクリーンショットの取得と、その画像の確認は別の作業として扱う。

## 参照した一次資料

- NIST, Sample sizes required: https://www.itl.nist.gov/div898/handbook/prc/section2/prc222.htm
- Playwright公式Page.goto: https://playwright.dev/docs/api/class-page#page-goto （同一文書の別hashへの移動はnullになる）

## 次の作業

これらの修正を検証用コピーへ適用し、全ビルド、数値、出典、基礎/2級ブラウザ検証を再実行する。成功した原稿と生成物をハッシュで固定し、画面確認後だけmainへ公開する。取得できていない実行結果を成功と見なさない。
