# 2級再開：保存内容の回収と検証用コピー

2026-09-11。確認した元コミット：`5ce8a296c61dbea3f69224b00dcd976f34634538`。最終実行 `34585919774` の失敗ログと原稿アーティファクトを実際に取得した。

## 確認できたこと

- 40講座の原稿が存在し、G39は取得済みUCIのCSVから計算結果を生成できている。8講座/32問のまま残っていた進捗フィールドは古い。
- 161ページの型検査・ビルド・内部リンクは成功したが、G25の数式組版がないことを検査が発見し、その後の公開工程を停止した。
- G25だけでなくG35・G38・G40も数式が通常の文字列のままだった。重要な数式を実際にKaTeXで組版できる表記へ修正する。検査条件は弱めない。
- 回収した原稿を別の作業コピーで確認し、143件の数値照合が成功した。これはまだ公開や全ブラウザ検査の成功ではない。

## 今回の修正

`resume-grade2-review.py` は全原稿を`review/grade2/20260911/submitted`へ保存し、`candidate`コピーで表示の修正を行う。検証に使うsrcへ同じ内容を移し、最終ゲート成功後だけ`verified`へ保存する。数値検証に記録した原稿ハッシュとの一致を必須にする。公開のmainへ移す工程は、この確認の後に行う。

数式の修正に加え、40講座/172問への状態整合、検索データのJSON埋め込み時のエスケープを修正する。基礎の原稿や端末保存キーは変更しない。

## 次の作業

全件の構造・数値・出典・ブラウザ検証を再実行し、画面を確認して修正する。検証済みの版だけをmainへ非強制で反映し、実公開ページと配信ファイルを確認する。公開後に準1級へ進む。未実施の人間による監修・学習効果試験を実施済みとしない。
