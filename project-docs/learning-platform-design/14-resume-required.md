# 全40講座の保存後：再開時に確認すること

作成日：2026-09-11

## 確認できているチェックポイント

基礎（4級・3級統合）のF01〜F40の原稿、F01〜F39各3問とF40の12問、合計129問を作成した。F39の再計算用CSVもある。全40講座がそろった保存コミットは `480161dbe2d02a034e3b7e8801963995198022e3`。この時点の対象ブランチは `content/foundation-complete-20260911`。main公開済みの6講座と混同しない。

最終段階では全講座の数値検証、SciPyによる別実装との照合、正規分布・頻度・信頼区間の操作型教材を追加する作業に進んだ。ただし、後半にツールからの応答内容を確認できない状態になったため、その追加分のコミット・ブランチ反映・検証成功・公開をこの文書では保証しない。実際のリポジトリ状態とActionsログを必ず読み直す。

## 再開時の順序

1. mainと検証ブランチの現在のHEAD、Actions実行結果、原稿40件と129問を読み直す。未検証の内容を公開しない。
2. `src/lib/learning-math.mjs`、`scripts/test-foundation-math.mjs`、`scripts/check-foundation-oracle.py` の実在・内容・テスト結果を確認する。存在しない場合は追加をやり直す。
3. `src/components/LearningLab.astro`、`src/pages/labs/`、`scripts/test-learning-labs.mjs` の実在と実動を確認する。新しく作ったtree/commitだけでブランチに載っていない場合を区別する。
4. 全講座の数値・図・式・解答・前提リンクを監査する。特にF14の箱ひげ図の最小値1の横位置は、他の点と同じ変換ならx=50であり、x=55のままなら修正する。F39の横長表は小画面で読みやすい縦表へ改めることを検討する。
5. 公式3級・4級の小項目を実際の節・問題へ対応させる。F04の乱数表の具体的な使用例、各種グラフの図示など、説明だけになっている内容を点検する。
6. 統計WEB・とけたろう・OpenIntro/OpenStax・Statlect・Seeing Theoryの実際の対応教材と比較し、自サイト側の不足と改善結果を文書化する。実測のない優越性を主張しない。
7. 既存Issue #1の依存関係監査警告をアドバイザリ単位で確認し、公開コードへの影響とビルド/開発のみの影響を区別する。強制的な破壊的更新は行わない。
8. README・AGENTS・基礎設計書・進捗を40講座の実際の状態へ更新する。全件ビルド、内部リンク、数式、PC/小画面、キーボード、開閉、印刷、操作型教材を再検証する。
9. 検証済み生成物を保存し、mainへ非強制で反映する。GitHub Pagesの公開完了と実ページを確認してから、基礎コースのリリース完了と報告する。

## 完了報告の条件

作成済み・保存済み・検証済み・公開済みを区別する。第三者の数学監修や利用者テストを行っていなければ未実施と記す。最終回答では、完成した範囲と次にやることを明示する。将来の2級・準1級・1級の全実装まで済んだとは言わない。
