# 統計ラボ：現行の制作・検証方針

更新日2026-09-11。旧方針の情報は[保存したAGENTS.md](project-docs/history/20260911-before-course-status/AGENTS.md)へ元のバイト列のまま残しています。以下が、サイト全体の対象・公開状態・次の作業に関する優先方針です。旧文書の用語辞典固有の編集・schema・キュー・固定役割・品質ルールは、用語辞典の編集時に引き続き適用します。旧文書の「2級のみ」「termsを常に最優先」「初期6講座のみ」は全体の対象を制限しません。

## 目標と現在地

学習順序は **基礎（4級・3級統合）→2級→準1級→1級**。独立した4級コースは設けません。初学者にも入口が分かり、上級者には必要な厳密さと証明が読めるサイトを目指します。

現在の公開範囲・未実装範囲は`project-docs/project-status.json`と各コースの公開記録で確認します。現在は基礎40、2級40、準1級6の86講座・326問。準1級は全72講座の予定中6講座を公開した段階であり、P07・P08の原稿が保存されていても公開本数へ加えません。1級は未実装です。

新しいユーザー指示は優先して反映し、進捗資料・本文・構成・公開版の違いをその都度確認します。古いチャットの成功報告だけで公開完了と判断しません。実行結果を読めなかったことと、変更が保存されなかったことも区別します。

## 正本を使い分ける

- 基礎：`src/content/lessons/`、`foundation-course.json`、`foundation-schema.ts`。
- 2級：`src/content/grade2/`、`grade2-course.json`、`grade2-schema.ts`。
- 準1級：`src/content/pregrade1/`、`pregrade1-course.json`、`pregrade1-schema.ts`。
- 用語辞典：`src/content/terms/`、`data/term-queue.json`、`src/content.config.ts`、`.agents/skills/term-page-generator/SKILL.md`。
- 公開状態：`project-docs/foundation-release/publication.json`、`project-docs/grade2/release/publication.json`、`project-docs/pregrade1/release/publication.json`。最新コースの記録で、共通ページを含む現在の配信を確認します。

上のcourse.jsonは`src/data/`、schema.tsは`src/lib/`を基準とします。講座のfrontmatterにはそれぞれのschemaを使い、用語記事用の旧キー禁止・主要情報をfrontmatterへ置く規則を、本文主体の新講座へ機械的に移しません。用語辞典では旧文書の必須キー、実在するrelated_terms、queueのcanonical値を維持します。既存URL・slug・公開原稿を無理由に置換しません。

## 一講座の品質

基本説明、直感、定義、使用条件、記号、数値例、図表の意味、問題、解答と理由、適切な詳説を一つの学習の流れにします。重要な使用条件・例外は閉じた説明だけに置かず、基本表示から読めるようにします。導出・証明・長い補足と解答は最初は閉じ、必要な人が開ける構成にします。

基礎は3問以上、2級・準1級は原則4問以上の独自問題を含めます。概念・計算・解釈を混ぜ、同型問題や字数を水増ししません。基本1500〜2500字、全体2500〜4500字程度は編集上の目安で、証明・図表の負荷と一つの問いの完結を優先します。

母数と観測値、確率変数と推定値、母分散の分母、独立と無相関、分布収束とモーメント収束、近似と厳密式を区別します。定義できない境界、密度の値域、分位点の向き、定理の前提を省略しません。根拠のない定理・同値関係・一般化を、もっともらしい文章で埋めません。未解消の重要事項はdraftにして理由を残します。

証明は途中の意味を追えるようにし、他の定理を使った導出ならその前提を明示します。シミュレーションや数値一致を、定理の証明と呼びません。実データは取得元、利用条件、版、単位、処理、ハッシュ、観察研究としての限界を記録します。架空データにはその旨を記します。

数式の重要な添字・指数・分数・総和はKaTeXで組版します。式の前後に説明を置き、必要以上に長い式へ詰め込みません。新しい補足説明で本文が読みにくくならないかも点検します。数学的事実は実際に確認した出典か、本文で前提から説明できる定義・式・計算で裏付けます。

## 確認の分担と表示

数学、初学者の説明、具体例・図、公式範囲と出典、編集、UIを分けて点検します。用語ページを扱うときは旧文書の固定6役割と統合手順も確認します。実際に起動していないsubagent、未実施の複数人監査・第三者監修を記録しません。自分による複数観点の確認は自己点検です。

競合の実際に読んだ代表教材を比較し、足りない箇所を修正します。競合に条件説明があるのに「ない」と比較しないこと、全編・動画・有料部分を読んでいないのに全体監査と書かないこと。学習効果の優越性は実証がなければ宣伝しません。文章・図・問題の転載や数値だけを替えた複製はしません。

## 画面と学習記録

ホーム・一覧は簡潔な導線とし、運用説明や大量の件数カードで埋めません。コース目次では、読める講座と準備中を正確に区別するために必要な本数だけ示します。未公開の空ページへリンクしません。目次、前提、前後の講座、問題と解答への直接リンクを維持します。

小画面で数値が途中改行しないこと、長い式・表がページ全体を横へ広げないこと、キーボード・印刷・JavaScript無効時の読解を点検します。折りたたみを開いた状態でも検査します。検索用テキストで数学の不等号をHTMLタグと誤認して本文を消さないこと。

端末内の確認マークと再開位置は明示的な操作だけで保存し、級ごとのキーを混ぜません。検索語や記録を外部に送りません。保存不可・壊れた記録でも基本の読解を止めず、削除は確認して当該コースだけに適用します。自己記録を理解度の自動認定と表示しません。

## 検証・公開・Git保存

原稿は数講座単位でそのターン内にcommit/pushし、現在地と次の作業を更新します。未公開草稿を公開コレクションの外へ安全に保存することと、未検証の画面をmainの公開物へ反映することは区別します。ユーザーが保存しないと指示した場合は従います。

原稿・UIの公開変更には`docs/`の検証済み生成物も含めます。`npm run generate:index`、`npm run verify`に加え、対象コースの構造・数値・ブラウザ検証と、既存コースの回帰を実行します。完全な手順は`.github/workflows/verify-pregrade1.yml`を参照してください。新規講座を足すときは件数・ID・問題数の検査対象を増やし、テストを削ったり期待値を根拠なく変更して通したりしません。

準1級の現行検証・公開処理は初期6講座に固定されているため、P07以降の組み込み時に実装済みIDと予定IDを分けた件数管理へ改修します。現在のP07・P08は`project-docs/pregrade1/drafts/`に保存し、ローカル21件の検算と未実施のブラウザ・公開工程を分けて記録しています。

検証に使用した原稿、生成物、実行結果はハッシュとコミットに結び付けます。後からmainが進んでいれば差分を照合し、force pushで上書きしません。実際の公開後にHTTP応答・講座ID・問題数・配信ハッシュを照合します。ボットによる検証候補コミットのskip-CI指定が昇格先の公開確認まで省略しないよう注意します。ワークフローを増やすだけで課題解決とせず、既存の失敗箇所を特定して直します。

依存関係の監査結果は対象のロックファイルと時点を記録します。ブラウザテスト用ツールの追加でサイトの依存を勝手に再解決しないよう、検証ツールを別の場所にインストールします。監査0件を将来の安全保証とは書きません。

最終報告では、実際にmainへ反映した内容・公開確認・未完成の範囲・次の作業を明示します。原稿があるだけで全範囲対応、全検証成功、公開完了とはしません。現時点の次はP07・P08の組み込み・全件検証・公開確認、その後P09以降です。準1級全72講座・1級までの長期目標は継続中です。
