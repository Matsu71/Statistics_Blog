name: Verify foundation content

on:
  push:
    branches: ['content/foundation-*']
  workflow_dispatch:

concurrency:
  group: foundation-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 1
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - name: Apply the versioned course-policy migration
        run: node scripts/migrations/20260911-foundation.mjs
      - name: Generate and verify the entire site
        run: |
          npm run generate:index
          npm run verify
          node scripts/validate-foundation.mjs
      - name: Test foundation pages in Chromium
        run: |
          npm install --no-save --package-lock=false playwright@1.55.0
          npx playwright install --with-deps chromium
          node scripts/test-foundation-browser.mjs
      - name: Record the verified site on the staging branch only
        if: startsWith(github.ref, 'refs/heads/content/foundation-')
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add docs src/generated/term-index.json AGENTS.md README.md project-docs/learning-platform-design
          if ! git diff --cached --quiet; then
            git commit -m 'build: regenerate verified foundation pages and record checks'
            git push origin HEAD:${{ github.ref_name }}
          fi
